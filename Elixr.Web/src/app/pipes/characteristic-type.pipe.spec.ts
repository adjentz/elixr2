import { CharacteristicTypePipe } from './characteristic-type.pipe';

describe('CharacteristicTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CharacteristicTypePipe();
    expect(pipe).toBeTruthy();
  });
});
